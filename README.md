# Lesterslist.com (Full Site Build)
Multi-page Node.js + MySQL Bluegrass community directory.
Includes Admin Panel, front-end pages, and database schema.
Run with:
  npm install
  npm start
Then open http://localhost:3000
